import { ComboboxWithHintVariantDirective } from './combobox-with-hint-variant.directive';

describe('OptionTemplateWithHintDirective', () => {
  it('should create an instance', () => {
    const directive = new ComboboxWithHintVariantDirective();
    expect(directive).toBeTruthy();
  });
});
