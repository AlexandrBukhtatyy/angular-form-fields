import { ComboboxDataProviderDirective } from './combobox-data-provider.directive';

describe('ComboboxDataProviderDirective', () => {
  it('should create an instance', () => {
    const directive = new ComboboxDataProviderDirective();
    expect(directive).toBeTruthy();
  });
});
