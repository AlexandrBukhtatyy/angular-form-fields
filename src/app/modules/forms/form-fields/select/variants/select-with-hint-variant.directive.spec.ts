import { SelectWithHintVariantDirective } from './select-with-hint-variant.directive';

describe('OptionTemplateWithHintDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectWithHintVariantDirective();
    expect(directive).toBeTruthy();
  });
});
